//! Generated protobuf types for `beemon.v1`. Tonic-build emits the file at
//! `${OUT_DIR}/beemon.v1.rs` (one `.rs` per proto package); we pull it in with
//! the `tonic::include_proto!` macro.

pub mod pb {
    tonic::include_proto!("beemon.v1");
}